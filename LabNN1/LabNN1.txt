Лабораторна робота з навчання нейромереж №1
Виконувати роботу краще у браузері Chrome

Крок 1.  
Файл main.js містить код побудови та навчання нейромережі, що порівнює два числа
Запустить index.html 
Переконайтеся, що число виведене в консолі є більшим за 0.9
Це результативність навченої мережі
Запишіть його

Перевірте на якісь парі чисел, як навчена мережа ї розпізнає
Для цього введіть у консоль 
window.network.calculateOutsForInputs([5,10])
window.network.getOutputs()

Ви побачите, якою буде активація вихідного шару нейромережі для вхідних чисел

Придумайте самі та спробуйте ще 3 приклади для вхідних даних. Запишіть для них активацію мережі

Крок 2.
Роздивимося мережу у деталях. 
Подайте на вхід числа 6 та 7:
window.network.calculateOutsForInputs([6,7])

Тепер виведемо мережу цілком
Для цього введіть команду 
window.network.export2Text()

Правою кнопкою по виведеному тексту - Copy String Content - і вставте дані з буфера у Excel
У Excel може знадобитися ще замінити точки на коми для кращого форматування

Крок 3.
Ви бачите, що значення вихідних нейронів не дорівнюють точно 0 та 1
Трошки підучимо мережу.
Вкажіть мережі, що для цих вхідних даних ідеальною активацієї вихідногшо слоя буде масив [0,1]
window.network.calculateErrors([0,1])

Тепер нехай мережа зробить підлаштовування вагових коефіцієнтів із коефіцієнтом 0.25 (щоб ефект підлаштовування був явніший)
window.network.adjustParams(0.25)

Знов подамо на вхід пару чисел 6 та 7 та роздивимося мережу в деталях

window.network.calculateOutsForInputs([6,7])
window.network.export2Text()

Цього разу вставте мережу на інший лист Excel

Порівняйте, як змінився вихід мережі після одного кроку навчання та як змінилися її внутрішні вагові коефіцієнти

Крок 4.
Дослідимо в деталях, як буде мінятися активація вихідних нейронів, якщо ми будемо міняти числа, що подаються на вхід
Будемо подавати на вхід пару чисел [6, x], де число х пробігатиме від 0 до 12 з кроком 0.1

var ar=[]
for (var x=0; x<=12; x+=0.1){
    window.network.calculateOutsForInputs([6,x])
    var out_ar = window.network.getOutputs()
    out_ar.splice(0,0,x)
    ar.push(out_ar)
}
console.table(ar)

Ви отримаєте таблицу, перша колонка якої - значеня х, а далі йдуть активації першого та другого нейронів для даного х
Побудуйте графіки активації нейронів в Excel

Крок 5.
Поекспериментуємо, як внутрішня будова мережі влине на її здатність розв'язувати задачу.

Навчимо нейромережу порівнювати числа по більш складносу алгоритму
Нехай вона також визначає випадок, коли числа приблизно дорівнюють один одному


Тоді на вихідному слої будуть 3 нейрони. 
Перший буде активуватися, якщо переше число перевищує друге більше ніж на 5%
Другий нейрон буде активуватися, якщо перше число знаходиться в межах 5% від другого
І третій нейрон активується, якщо перше число менше, ніж друге з врахуванням похибки у 5%

Що для цього потрібно модифікувати в скрипті навчання?

По-перше, в мережі потрібно зробити 3 вихідних нейрони

Далі модифікуємо функцію, яка визначає правильну відповідь мережі при її навчанні
Вона має повертати 0, якщо перше число більше ніж друге, збільшене на 5%
повертати 2, якщо першее число менше ніж перше число, зменшене на 5%
і повертати 1 в усіх інших випадках
Ось що в ній має бути:
		let x = input_arr[0]
		let y = input_arr[1]
		if (x>=y+0.05*Math.abs(y)){
			return 0
		}else{
			if (x<=y-0.05*Math.abs(y)){
				return 2
			}else{
				return 1
			}			
		}
зверніть увагу, що для визначення меж ми беремо модуль від числа, щоб код працював і для від'ємних чисел

Зміни потрібні в генерації навчальної вибірки. Ми не можемо тепер обидва вхідних числа брати випадковими, і ось чому
У навчальній вибірці потрібно в рівній мірі відобразити всі ситуації, з якими мережа стикнеться
А якщо генерувати числа випадково, то випадок наближеної рівності (в межах 5%) буде дуже рідко зустрічатися

Отже, генерацію навчальних даних проведемо таким чином
Згенеруємо друге число випадково.
перше число згенеруєхмо у межах відхилення 5% від першого
Далі, у 33% випадків перегенеруємо перше число так, щоб воно було явно мернше другого
У ще у 33% випадків перегенеруємо перше число так, щоб воно було явно більше другого

    	let y = Math.random()*20-10
		let x = y+Math.abs(y)*(0.05-0.1*Math.random())
		let rnd = Math.random()
		if (rnd<0.33){
			x=y-Math.abs(y)*0.05-Math.random()*20
		}
		if (rnd>0.67){
			x=y+Math.abs(y)*0.05+Math.random()*20
		}

Так само будемо поступати при генерації тестових даних. З тією лише відмінністю, що діапазон для них візьмемо від -1000 до 1000 (щоб перевірити, як мережа навчилася узагальнювати поняття порівняння)

        let y = Math.random()*2000-1000
		let x = y+Math.abs(y)*(0.05-0.1*Math.random())
		let rnd = Math.random()
		if (rnd<0.33){
			x=y-Math.abs(y)*0.05-Math.random()*2000
		}
		if (rnd>0.67){
			x=y+Math.abs(y)*0.05+Math.random()*2000
		}