**Інструкції до виконання лабораторної роботи "Аналіз графу ігрових позицій"**
У даній лабораторній роботі ми ознайомимося з ботом, який виконує аналіз всіх ігрових позицій, починаючи з позиції, якою завершується гра

**Крок 1**
Відкрийте папку Lab3. Запустіть файл index.html у браузері та відкрийте консоль браузера.
На старті роботи скрипта main.js створюється ігровий світ для гри Баше, у якій допускається брати 1, 2 чи 3 камінці та виграє той, хто забирає останній предмет.

У файлі bots.js є бот UniversalBot. Він на початку партії будує виграшну стратегію, і протягом партії слідує неї
Влаштуйте турнір між ним, BachetBot123Best (який має жорстко закодовану стратегію для даних правил гри) та BachetBotRandomSmart (який робить випадковий хід)
window.world.startTournamentBetweenBots([new UniversalBot("Universal"), new BachetBot123Best("Best"), new BachetBotRandomSmart("RND")], 10000, 2, false)

Впевніться, що UniversalBot грає на рівні з BachetBot123Best та вони обидва повністю обігрують BachetBotRandomSmart
Запишіть результати цього турніру
**Крок 2**
Модифікуємо правила гри, дозволивши брати 1, 2, 3 або 4 камінці
window.world.allowedMoves=[1,2,3,4]

І знов запустіть такий же турнір. Запишіть його результати. Як ви їх поясните (чому боти зіграли саме з таким рахунком)?

**Крок 3**
BachetBotRandomSmart робить випадкові ходи тільки з діапазону 1, 2, 3. Він також не знає про те, що правила гри можуть змінитися
Створіть бота UniversalRandomBot, які буде робити ходи, що не порушують правил, але брати їх з правил поточної гри
Функція ходу для цього бота матиме вигляд:

	makeMoveForSituation(gameDataOb){
		var selectedMoveVal = -1;
		//перебираємо всі дозволені правилами ходи та обираємо випадковий
		for (var i=0; i<this.currentGameRulesObject.allowedMoves.length; i++){
			var moveVal = this.currentGameRulesObject.allowedMoves[i];
			var newPos = gameDataOb.N-moveVal;	
			if (newPos>=0){
				if (selectedMoveVal==-1 || Math.random()<1/(i+1)){
					selectedMoveVal = moveVal;
				}
			}
		}
		return {n:selectedMoveVal}
	}


Проведіть турнір для 4х ботів спочатку для стандартних правил гри
window.world.startTournamentBetweenBots([new UniversalBot("Universal"), new BachetBot123Best("Best"), new BachetBotRandomSmart("RND"), new UniversalRandomBot("URND")], 10000, 2, false)
А потім - для правил, за якими дозволено брати 1,2,3 або 4

Запишіть результати обох турнірів та поясніть їх

**Крок 4**
UniversalBot на даний момент прораховує виграшні та програшні позиції тільки для умов, коли той, хто забирає останній камінь, перемагає. 
Перевірте, як він гратиме якщо у правилах встановити 
window.world.isLastPlayerWinner=false

І знов проведіть турнір чотирьох ботів.

Запишіть його результати. 

Модифікуйте код UniversalBot так, щоб він правильно оцінював, чи є позиція виграшною чи програшною і для правил гри, коли останній хід веде до програшу
Подумайте, як це зробити

Зробивши, запустіть попередній турнір на правилах, коли можна брати 1, 2, 3, 4 камінці, а останній хід веде до програшу
Запишіть результати цього турніру

**Крок 5**
Результати аналізу позицій, який зробив UniversalBot, можна побачити у консолі
Для цього у його функції getInformedOfGameStart розкоментуйте рядки 
console.log("аналіз позицій")
console.log(this.analysisOfpositions)
Щоб побачити, як бот UniversalBot визначає виграшні та програшні позиції, проведіть  за стандартними правилами турнір, що складається з однієї партії між UniversalBot та будь-яким іншим ботом
Випишіть значення масиву проаналізованих позицій для перших 21й позицій у таблицю:

N	Тип позиції
0	П
1	В
.....

У третій колонці визначте, який хід буде виграшним

**Крок 6**
Подивіться, як UniversalBot аналізуватиме наступні ігри:
можна брати 1, 2, 3 і той, хто забирає останній камін програє
можна брати 1, 2, 3, 4 і той, хто забирає останній камінь виграє
можна брати 1, 3, 4 і той, хто забирає останній камінь виграє

Для кожної гри сформулюйте узагальнююче правило - як визначити виграшний хід з позиції?

**Крок 7**
Індивідуальне завдання (обирається за номером K у журналі)
Подивіться, як UniversalBot аналізуватиме гру, у якій можна брати K%5+1, (2*K)%7+2, (3*K%11)+3 камінців і той, хто робить останній хід виграє (якщо K парне) і програє якщо K непарне
Сформулюйте узагальнююче правило визначення виграшних ходів для цієї гри

**Крок 8**
Зробіть бота, який буде звертатися за ходом до гравця
Назвіть його HumanBot
Його функція ходу виглядатиме так:

	makeMoveForSituation(gameDataOb){
		var s = "Bot moves: "+this.myName+"\n"
		s+="Current situation: "+gameDataOb.N+"\n";
		s+="You can take: "+this.currentGameRulesObject.allowedMoves+"\n"

		var selectedMoveVal = prompt(s)*1;
		
		return {n:selectedMoveVal}
	}

Додайте також функції, які будуть інформувати гравця-людину про виграш чи програш

Проведіть турнір з однієї гри між HumanBot та UniversalRandomBot. Чи зможете ви виграти цю гру, користуючися виведеними раніше правилами?